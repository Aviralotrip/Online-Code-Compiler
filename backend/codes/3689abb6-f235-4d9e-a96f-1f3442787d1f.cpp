#include <iostream>
#include <bits/stdc++.h>
int main() {
    for(int i = 0; i < 6; i++) {
         { 
            std::cout << "Iteration: " << i << std::endl; 
        
    }
    return 0; // Moved return statement outside the outer loop
}